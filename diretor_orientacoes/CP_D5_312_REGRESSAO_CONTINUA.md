# CP-D5-312 — Regressão contínua da sequência R1–R6

A cena `CartographicSequenceR1R6Preview.tscn` passou parser (`PARSER_EXIT=0`) e sessão QA headless de 36 segundos (`QA_EXIT=124`, timeout controlado). A telemetria reconfirmou `status=approved`, `checks=8`, `no_anchor_skip=true`, `majestic_lateral=true`, `ruins_arrival=true` e o handoff final para Vila Elevada.

Não foram observados erros de parser ou script. Os avisos FSR1 são específicos do renderer OpenGL Compatibility headless. Evidência completa: `CP-D5-312_RUNTIME.log`.
